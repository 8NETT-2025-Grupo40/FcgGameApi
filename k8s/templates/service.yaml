apiVersion: v1
kind: Service
metadata:
  name: {{ include "fcg-game-api.fullname" . }}
  labels: {{- include "fcg-game-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "fcg-game-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
